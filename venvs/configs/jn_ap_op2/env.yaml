# task_perturbation_prediction src/methods/jn_ap_op2/config.vsh.yaml: image base_pytorch_nvidia:1.0.0,
# setup: fastparquet, pandas, category_encoders. Base has torch, anndata~=0.11.0.
name: jn_ap_op2
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python==3.10
  - numpy
  - pandas
  - tqdm
  - pytorch::pytorch=2.4.0
  - nvidia::cuda-toolkit=12.5.0
  - pip
  - pip:
    - anndata~=0.11.0
    - category-encoders
    - fastparquet
